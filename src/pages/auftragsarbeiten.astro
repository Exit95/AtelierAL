---
import Layout from "../layouts/Layout.astro";
import ContactForm from "../components/ContactForm.astro";
---

<Layout
    title="Auftragsarbeiten"
    description="Individuelle Kunstwerke nach Ihren Wünschen. Katharina Lanvermann erschafft abstrakte Kunst, die perfekt zu Ihren Räumen passt."
>
    <section class="page-hero">
        <div class="container">
            <h1 class="hero-title">AUFTRAGSARBEIT</h1>
            <p class="hero-subtitle">
                Ein Kunstwerk für dich, individuell gefertigt.
            </p>
        </div>
    </section>

    <!-- Intro Text -->
    <section class="section py-12">
        <div class="container text-center max-w-3xl mx-auto">
            <p class="intro-text">
                Jedes Kunstwerk lässt mich wie einen visuellen
                Geschichtenerzähler fühlen – ich lasse Kunst, Stil und Gefühl
                miteinander verschmelzen.
            </p>
        </div>
    </section>

    <!-- Process Steps -->
    <section class="section py-16 bg-surface">
        <div class="container">
            <div class="process-grid">
                <div class="process-step">
                    <h3 class="step-title">ANFRAGE & ERSTGESPRÄCH</h3>
                    <p class="step-desc">
                        Ein Kunstwerk für dich, individuell gefertigt.
                    </p>
                    <p class="step-details">
                        Sende mir eine Anfrage mit deinen Vorstellungen. Teile
                        mir deine Farbwünsche, bevorzugten Stile und Raummaße
                        mit. Optional kannst du auch Referenzbilder hinzufügen.
                    </p>
                </div>

                <div class="process-step">
                    <h3 class="step-title">KONZEPTENTWICKLUNG</h3>
                    <p class="step-desc">Ein individuelles Angebot für dich.</p>
                    <p class="step-details">
                        Basierend auf deinen Wünschen erstelle ich ein
                        Farbkonzept und bespreche mit dir die Details. Gemeinsam
                        entwickeln wir eine Vision für dein individuelles
                        Kunstwerk.
                    </p>
                </div>

                <div class="process-step">
                    <h3 class="step-title">UMSETZUNG</h3>
                    <p class="step-desc">Visualisierung & Fertigung.</p>
                    <p class="step-details">
                        Ich erschaffe dein Werk mit großer Sorgfalt und
                        Leidenschaft. Der Entstehungsprozess dauert je nach
                        Größe und Komplexität zwischen 2 und 6 Wochen.
                    </p>
                </div>

                <div class="process-step">
                    <h3 class="step-title">ÜBERGABE</h3>
                    <p class="step-desc">Abholbereit.</p>
                    <p class="step-details">
                        Nach Fertigstellung kannst du dein Kunstwerk im Atelier
                        abholen oder ich organisiere die Lieferung zu dir.
                    </p>
                </div>
            </div>
        </div>
    </section>

    <!-- Pricing Info -->
    <section class="section py-16">
        <div class="container">
            <div class="pricing-box">
                <p class="pricing-text">
                    Für individuelle Auftragsarbeiten erstelle ich dir gerne ein
                    persönliches Angebot.
                    <br /><br />
                    Die Preise richten sich nach Größe und Aufwand.
                    <br />
                    Kleine Formate ab 450€ | Mittlere Formate ab 800€
                </p>
            </div>
        </div>
    </section>

    <!-- Contact Section -->
    <section class="section py-16 bg-surface">
        <div class="container">
            <ContactForm formTitle="Direkte Anfrage" />
        </div>
    </section>
</Layout>

<style>
    /* Hero Section */
    .page-hero {
        padding: var(--space-24) 0 var(--space-12);
        text-align: center;
        background-color: var(--color-background);
    }

    .hero-title {
        font-family: var(--font-serif);
        font-size: clamp(3.5rem, 6vw, 6rem);
        font-weight: 400;
        letter-spacing: 0.05em;
        text-transform: uppercase;
        margin-bottom: var(--space-4);
        color: var(--color-text-primary);
        line-height: 1.1;
    }

    .hero-subtitle {
        font-family: var(--font-sans);
        font-size: var(--font-size-lg);
        text-transform: uppercase;
        letter-spacing: 0.1em;
        color: var(--color-text-secondary);
        margin-top: var(--space-4);
    }

    /* Intro Text */
    .intro-text {
        font-family: var(--font-serif);
        font-size: var(--font-size-2xl);
        line-height: 1.6;
        color: var(--color-text-primary);
        max-width: 900px;
        margin: 0 auto;
    }

    /* Process Section */
    .bg-surface {
        background-color: var(--color-surface);
    }

    .process-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: var(--space-12);
    }

    .step-title {
        font-family: var(--font-serif);
        font-size: var(--font-size-xl);
        text-transform: uppercase;
        letter-spacing: 0.05em;
        margin-bottom: var(--space-2);
        border-bottom: 1px solid var(--color-text-primary);
        padding-bottom: var(--space-2);
        display: inline-block;
    }

    .step-desc {
        font-family: var(--font-serif);
        font-size: var(--font-size-xl);
        color: var(--color-text-secondary);
        margin-bottom: var(--space-4);
        font-style: italic;
    }

    .step-details {
        font-family: var(--font-sans);
        font-size: var(--font-size-base);
        line-height: 1.6;
        color: var(--color-text-secondary);
    }

    /* Pricing Box */
    .pricing-box {
        text-align: center;
        max-width: 800px;
        margin: 0 auto;
        padding: var(--space-12);
        border: 1px solid var(--color-border);
        background-color: var(--color-surface-elevated);
    }

    .pricing-text {
        font-family: var(--font-serif);
        font-size: var(--font-size-lg);
        line-height: 2;
    }

    /* Contact Section */
    .contact-content-centered {
        text-align: center;
        max-width: 800px;
        margin: 0 auto;
    }

    .contact-title {
        font-family: var(--font-serif);
        font-size: 3.5rem;
        text-transform: uppercase;
        margin-bottom: var(--space-8);
        line-height: 1.1;
        letter-spacing: 0.05em;
    }

    .contact-links {
        display: flex;
        flex-direction: row;
        justify-content: center;
        gap: var(--space-8);
        margin-bottom: var(--space-8);
    }

    .contact-link {
        font-family: var(--font-sans);
        font-size: var(--font-size-lg);
        text-decoration: none;
        color: var(--color-text-primary);
        transition: color var(--transition-base);
    }

    .contact-link:hover {
        color: var(--color-accent);
    }
</style>
