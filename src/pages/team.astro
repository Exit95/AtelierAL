---
import Layout from "../layouts/Layout.astro";
import { getAllArtworks, type Artwork } from "../lib/database";

// Lade die letzten 4 Werke für "Bisherige Arbeit"
const allArtworks = getAllArtworks() as Artwork[];
const recentArtworks = allArtworks.slice(0, 4);
---

<Layout
    title="Über mich"
    description="Katharina Lanvermann - Abstrakte Künstlerin aus Ahaus. Erfahren Sie mehr über mich und meine künstlerische Vision."
>
    <!-- Über Mich Section -->
    <section class="section dark-section" style="padding-top: 8rem; padding-bottom: 4rem;">
        <div class="container">
            <div class="content-grid">
                <div class="text-content">
                    <h1 class="section-title-mixed">
                        <span class="title-mastro">ÜBER</span>
                        <span class="title-fugu">mich</span>
                    </h1>
                    <div class="text-body">
                        <p>
                            Seit ich denken kann, bin ich kreativ. Malen ist für
                            mich mehr als nur Farbe auf Leinwand zu bringen. Es
                            ist ein Ausdruck meiner Gefühle, eine Reise in mein
                            Inneres und eine Möglichkeit, die Welt um mich herum
                            zu verarbeiten.
                        </p>
                        <p>
                            In meinem Atelier in Ahaus entstehen abstrakte
                            Kunstwerke, die durch ihre Farben und Formen
                            bestechen. Ich liebe es, mit verschiedenen
                            Materialien zu experimentieren und neue Techniken
                            auszuprobieren.
                        </p>
                        <p>
                            Die Natur ist meine größte Inspirationsquelle. Die
                            Farben der Jahreszeiten, das Licht und die Schatten,
                            die Strukturen und Formen – all das fließt in meine
                            Bilder ein.
                        </p>
                        <p>
                            Ich lade dich ein, meine Galerie zu besuchen und
                            dich von meinen Werken inspirieren zu lassen.
                            Vielleicht findest du ja ein Bild, das dich berührt
                            und das du gerne in deinem Zuhause haben möchtest.
                        </p>
                    </div>
                </div>
                <div class="image-content">
                    <img
                        src="/kuenstlerfoto.jpg"
                        alt="Katharina Lanvermann"
                        class="about-image"
                    />
                </div>
            </div>
        </div>
    </section>

    <!-- Trust / Stats Section -->
    <section class="section stats-section">
        <div class="container">
            <div class="stats-grid">
                <div class="stat-item">
                    <span class="stat-number">Ahaus</span>
                    <span class="stat-label">Atelier-Standort</span>
                </div>
                <div class="stat-item">
                    <span class="stat-number">Acryl & Mixed Media</span>
                    <span class="stat-label">Technik-Schwerpunkt</span>
                </div>
                <div class="stat-item">
                    <span class="stat-number">Workshops & Kurse</span>
                    <span class="stat-label">Für Anfänger & Fortgeschrittene</span>
                </div>
                <div class="stat-item">
                    <span class="stat-number">Auftragsarbeiten</span>
                    <span class="stat-label">Individuelle Kunstwerke</span>
                </div>
            </div>
        </div>
    </section>

    <!-- Bisherige Arbeit Section (Preview) -->
    <section class="section py-16 light-section">
        <div class="container">
            <h2 class="section-title-center handwriting">Bisherige Arbeit</h2>
            <div class="gallery-grid">
                {recentArtworks.map((artwork) => (
                    <a href={`/werke/${artwork.id}`} class="gallery-item">
                        <img
                            src={artwork.images?.[0] || '/placeholder.jpg'}
                            alt={artwork.title}
                            loading="lazy"
                        />
                    </a>
                ))}
            </div>
        </div>
    </section>

    <!-- Quote Section -->
    <section class="section py-16 dark-section">
        <div class="container">
            <div class="quote-container">
                <p class="quote-text">
                    "Jedes Kunstwerk lässt mich wie einen visuellen
                    Geschichtenerzähler fühlen – ich lasse Kunst, Stil und
                    Gefühl miteinander verschmelzen."
                </p>
            </div>
        </div>
    </section>
</Layout>

<style>
    .dark-section {
        background-color: var(--color-dark);
        color: var(--color-text-light);
    }

    .light-section {
        background-color: var(--color-light);
        color: var(--color-text-dark);
    }

    .content-grid {
        display: grid;
        grid-template-columns: 1fr;
        gap: var(--spacing-lg);
        align-items: start;
    }

    @media (min-width: 960px) {
        .content-grid {
            grid-template-columns: 1fr 1fr;
        }
    }

    .section-title {
        font-family: var(--font-serif);
        font-size: clamp(3rem, 6vw, 5rem);
        line-height: 0.9;
        margin-bottom: var(--spacing-md);
        font-weight: 400;
    }

    .section-title.handwriting {
        font-family: 'Mastro Display', serif;
        font-weight: 800;
        line-height: 1.1;
        color: white;
        letter-spacing: 0.05em;
    }

    /* Mixed title styles inherited from global .section-title-mixed */
    .section-title-mixed {
        color: var(--color-light);
    }

    .title-mastro {
        color: var(--color-light);
    }

    .title-fugu {
        color: var(--color-white);
    }

    .section-title-center.handwriting {
        font-family: var(--font-headline);
        font-size: var(--font-size-4xl);
        font-weight: 800;
        line-height: var(--line-height-tight);
        letter-spacing: var(--letter-spacing-wide);
    }

    .text-body p {
        margin-bottom: var(--spacing-sm);
        font-size: 1.1rem;
        line-height: 1.8;
        color: var(--color-light);
    }

    .about-image {
        width: 100%;
        aspect-ratio: 3/4;
        object-fit: cover;
        border-radius: 2px;
        box-shadow: var(--shadow-lg);
    }

    .section-title-center {
        font-family: var(--font-serif);
        font-size: clamp(2rem, 4vw, 3rem);
        text-align: center;
        margin-bottom: var(--spacing-lg);
        font-weight: 400;
        letter-spacing: 0.05em;
    }

    .gallery-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: var(--spacing-sm);
    }

    @media (min-width: 768px) {
        .gallery-grid {
            grid-template-columns: repeat(4, 1fr);
            gap: var(--spacing-md);
        }
    }

    .gallery-item img {
        width: 100%;
        height: 100%;
        object-fit: cover;
        aspect-ratio: 1;
        transition: transform 0.5s ease;
    }

    .gallery-item {
        overflow: hidden;
    }

    .gallery-item:hover img {
        transform: scale(1.05);
    }

    /* Stats Section */
    .stats-section {
        background-color: var(--color-surface-warm);
        padding: var(--space-12) 0;
    }

    .stats-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: var(--space-8);
        text-align: center;
    }

    @media (min-width: 768px) {
        .stats-grid {
            grid-template-columns: repeat(4, 1fr);
        }
    }

    .stat-item {
        display: flex;
        flex-direction: column;
        gap: var(--space-2);
    }

    .stat-number {
        font-family: var(--font-headline);
        font-size: var(--font-size-lg);
        font-weight: 700;
        color: var(--color-accent);
        letter-spacing: var(--letter-spacing-wide);
    }

    .stat-label {
        font-size: var(--font-size-xs);
        color: var(--color-text-secondary);
        text-transform: uppercase;
        letter-spacing: var(--letter-spacing-wider);
    }

    .quote-container {
        max-width: 800px;
        margin: 0 auto;
        text-align: center;
    }

    .quote-text {
        font-family: var(--font-fugu);
        font-size: var(--font-size-3xl);
        font-weight: 500;
        line-height: 1.5;
        color: var(--color-light);
    }
</style>
